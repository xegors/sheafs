# AGENTS.md

This is not a software project. It is an archive of scanned handwritten lecture notes (sheaf theory, in Russian, Lvivski course — Lecture 1+ visible).

- Content: `*.jpg` scans of a grid notebook are the source of truth. Do not delete, overwrite, recompress, or rename them.
- No code, build, test, lint, typecheck, CI, or package manager exists. Do not scaffold any (no `package.json`, `pyproject.toml`, `Makefile`, etc.) unless explicitly asked.
- No `README`, manifest, or OpenCode config to reconcile; this file is the only instruction source.
- Working with notes: read images directly; transcription/translation output (if requested) goes in new files, never edits to the `.jpg` originals.
- Filenames (`5287699...jpg`) are scan IDs, not page order — verify page numbers from handwritten foliation before assuming sequence.
